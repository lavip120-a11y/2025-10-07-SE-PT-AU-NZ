const r = 0xff;
const g = 0xff;
const b = 0xff;

const white = `rgb(${r}, ${g}, ${b})` //hexadecimal representation
console.log(white) // rgb(255, 255, 255) // converts the hexadecimal number into number for the colour